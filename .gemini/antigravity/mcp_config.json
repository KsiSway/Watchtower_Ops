{
  "mcpServers": {
    "postgresql": {
      "command": "docker",
      "args": [
        "run",
        "--add-host=host.docker.internal:host-gateway",
        "mcp-server-postgres"
      ]
    }
  }
}
