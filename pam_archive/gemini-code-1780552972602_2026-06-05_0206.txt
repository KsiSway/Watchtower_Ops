[URGENT DIRECTIVE FOR ANTIGRAVITY]
Target File: D:\Watchtower_Ops\watchtower_gui.py

Execute the "RFC 1918 Security Interlock" rewrite immediately:
1. UI Update: Replace the 'Autonomous Hunt' selectbox with a 4-column Streamlit checkbox matrix (`st.columns(4)`) supporting all 12 OSINT bridges.
2. Execution Loop: Refactor `run_omni_sweep(target, active_scripts: list)`. You MUST retain `await process.communicate()` to prevent host buffer deadlocks.
3. Safety Interlock: Import `ipaddress`. Inside `run_omni_sweep`, evaluate the target. If `ipaddress.ip_address(target).is_private` is true, drop 'osint_shodan_bridge.py', 'osint_zoomeye_bridge.py', 'osint_crtsh_bridge.py', and 'osint_leakcheck_bridge.py' from the active queue.
4. Routing Logic: Implement a `try/except ValueError` block for the IP evaluation. If the target is a domain/username (raising ValueError), assume it is a public target and bypass the drop logic.

Acknowledge when the patch is committed.